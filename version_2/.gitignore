# Operating system and editor files
.DS_Store
Thumbs.db
*.swp
*.swo

# Python caches and local environments
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_tmp/
.mypy_cache/
.ruff_cache/
.venv/
venv/
.env
build/
dist/
site/
*.egg-info/

# Native build artifacts
*.dll
*.exe
*.obj
*.o
*.so
*.dylib

# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.toc

# Generated project outputs
outputs/
# Un-ignore static reference files inside validation/outputs that are committed
# as ground-truth documents (not generated artifacts).
!validation/outputs/
!validation/outputs/**/.gitignore
!validation/outputs/integrator_method_validation/
!validation/outputs/integrator_method_validation/integrator_method_validation_summary.json
runs*/
hidden_verify/
chua_piecewise/
/native/
artifacts/reference_runs/native/
.tmp.driveupload/
__codex_dir_test/
.runtime_cache/
.runtime_native/

# pytest-benchmark baselines (platform-specific; store explicitly when needed)
.benchmarks/

# Decide if outputs_unified/ should be versioned or not.
# If you want to version it (recommended to preserve unifications and manifests), keep it unignored.
# If you want to ignore it (e.g., to avoid committing 2+ GB of trajectory files), uncomment the line below:
# outputs_unified/
