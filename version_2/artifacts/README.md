# Artifacts

This folder holds migrated runtime artifacts and prebuilt binaries from the old
project layout.

The active package compiles native C sources from
`hidden_attractors/native/csrc/` into `.runtime_native/` when needed. Treat files
under `artifacts/` as reference material, not as public API.
