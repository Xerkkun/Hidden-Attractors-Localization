# Release sample output

These JSON files describe expected lightweight command outcomes. They are not generated validation evidence and do not claim a scientific result.

`sample_status` is currently `template_outputs_recorded`; these files record lightweight expected command outcomes for v1.0.0 and remain non-evidence templates for release checks.
