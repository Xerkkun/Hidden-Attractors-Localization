# Remaining work

The software package is prepared for release metadata and documentation review.
The following items remain intentionally declared rather than hidden:

- Complete selected validation runs.
- Regenerate the final scientific freeze audit after the evidence set is fixed.
- Replace template sample outputs with executed outputs if the archive requires executed samples.
- Keep non-promoted arctan and Lyapunov discrepancy lanes labeled conservatively.
- Treat `BLOCKING_EVIDENCE_GAPS.md` and `BLOCKING_RELEASE_ITEMS.md` as blockers for declaring `v1.0.0`.
