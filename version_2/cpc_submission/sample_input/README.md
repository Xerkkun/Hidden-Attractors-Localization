# CPC sample input

These files are lightweight templates for CPC reviewers. They use the public `hidden-attractors` CLI and write only to ignored local output folders under `version_2/outputs/cpc_samples/`.

The sample configurations are intentionally small and are not promoted validation evidence. They are provided to inspect the package interface and configuration schema.
