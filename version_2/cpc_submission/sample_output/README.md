# CPC sample output

These JSON files describe expected lightweight command outcomes for reviewers. They are not generated validation evidence and do not claim a scientific result.

`sample_status` is currently `template_only_pending_execution`; replace these files with actual command outputs only after executing and recording the sample commands on the final CPC-preparation commit.
