# Hiddenness Controls

The corrected EFORK-3 `q=1` hiddenness rerun probes seven radii around each of `E0`, `E+`, and
`E-`, with 24 samples per radius. The stored run has 504 detailed probe rows
and reports zero trajectories classified as `TARGET`.

This supports the designation "numerical hidden-attractor candidate" under the
recorded finite probes and integration horizons. It is not an all-neighborhood
mathematical proof.

Neither the symbolic Wolfram/Mathematica script nor the supplied MATLAB seed
script independently reruns these 504 neighborhood classifications. This
stage is validated against the regenerated Python/native run artifacts.
The report's earlier integration-dependent values are retained only as
historical context; fractional-case independent validation scripts will be
incorporated separately when supplied.
