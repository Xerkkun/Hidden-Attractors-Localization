# Numerical contract

Corrida `rerun_candidates_long_h001_t300` con EFORK C corregido (`K3 = h^q*f(x_n + a31*K1 + a32*K2)`) como backend de barrido y ABM full-history como referencia de robustez. La truncacion de memoria se compara como variante numerica y no sustituye al benchmark de historia completa.
