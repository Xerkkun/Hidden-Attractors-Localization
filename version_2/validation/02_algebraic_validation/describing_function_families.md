# Describing-Function Families

The manual Lur'e split reproduces the non-smooth vector field. The two centered branches at `q=0.9998` match MATLAB after normalizing the transfer sign: Python uses `1 + k*W_code = 0`, while the report/MATLAB form uses `1 - k*W_report = 0` with `W_code = -W_report`.

This stage produces harmonic seeds only; it does not establish a bounded chaotic trajectory or hiddenness.
